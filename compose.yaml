services:
  switch-manager:
    build: .
    container_name: switch-manager
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      DATA_DIR: /app/data
      NITRO_HOST: 0.0.0.0
      NITRO_PORT: 3000
    volumes:
      - ./data:/app/data
