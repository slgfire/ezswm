# pnpm: refuse to install packages younger than this (minutes).
# 1 week = 10080 minutes. Supply-chain safety net against
# freshly-published malicious versions.
minimum-release-age=10080

# Hoist Nuxt / Vue ecosystem peer deps to the workspace root so
# Nuxt's auto-import scanner finds them.
shamefully-hoist=true
